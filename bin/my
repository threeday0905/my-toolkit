#!/usr/bin/env node

'use strict';

var program = require('commander');

program
    .version( require('../package.json') )
    .command('init', 'create project sample')
    .command('setup', 'setup project sample')
    .parse(process.argv);
